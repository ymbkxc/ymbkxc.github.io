# 六时杂谈 · 博客说明文档

> 线上地址：https://ymbkxc.github.io/
> 本地项目：`~/Documents/ymbkxc.github.io`

---

## 一、博客架构

| 组件 | 说明 |
|------|------|
| **Hexo** | 静态站点生成器，把 Markdown 转成网页 |
| **GitHub Pages** | 托管静态网页，访问 `ymbkxc.github.io` 即可看博客 |
| **hexo-deployer-git** | Hexo 插件，一键把生成的网页推到 GitHub |
| **landscape** | 当前主题（Hexo 默认主题） |

## 二、分支结构（重要）

仓库 `ymbkxc/ymbkxc.github.io` 上有 3 个分支：

| 分支 | 作用 | 谁更新 |
|------|------|--------|
| `source` | **Hexo 源码**：文章 `.md`、配置 `_config.yml`、主题 | 你手动 `git push` 备份 |
| `hexosub` | **生成的静态网页**：GitHub Pages 读这个分支 | `./deploy.sh` 自动推 |
| `master` | 旧静态站存档（不再使用） | 不动 |

> 即使本地文件丢失，只要 `source` 分支在，源码就能恢复。

## 三、本地目录结构

```
~/Documents/ymbkxc.github.io/
├── _config.yml              # Hexo 主配置（站点标题、URL、部署配置）
├── package.json             # 依赖清单
├── deploy.sh                # 一键发布脚本
├── new.sh                   # 新建文章脚本
├── source/
│   └── _posts/              # ← 你的博客文章都在这里（.md 文件）
│       ├── 金刚般若波罗蜜经.md
│       ├── LSTM理解.md
│       ├── LSTM知识点.md
│       ├── Pytorch知识点.md
│       └── 主成分分析故障诊断.md
├── themes/                  # 主题目录
├── public/                  # 生成的静态网页（hexo generate 产出，可删）
└── db.json                  # 缓存数据库（可删）
```

**你最常打交道的只有两个目录/文件：**
- `source/_posts/` —— 放新文章
- `_config.yml` —— 改站点配置（标题、作者等）

## 四、现有文章清单

| 标题 | 发布日期 | 分类 | 标签 |
|------|----------|------|------|
| 金刚般若波罗蜜经 | 2021-09-16 | 经典 | 佛经、经典 |
| LSTM理解 | 2019-12-19 | 技术 | LSTM、deeplearning |
| LSTM知识点 | 2019-12-19 | 技术 | LSTM、deeplearning |
| Pytorch知识点 | 2019-12-19 | 技术 | pytorch |
| 主成分分析故障诊断 | 2019-12-19 | 技术 | PCA |

## 五、常用命令速查

| 操作 | 命令 |
|------|------|
| 新建文章 | `./new.sh 文章标题` |
| 本地预览 | `hexo server`（浏览器开 http://localhost:4000） |
| 发布博客 | `./deploy.sh` |
| 清理重生成 | `hexo clean && hexo generate` |
| 备份源码 | `git add . && git commit -m "msg" && git push origin source` |

## 六、文章格式说明

每篇文章是 `source/_posts/` 下的一个 `.md` 文件，开头是 frontmatter（两行 `---` 之间的配置），下面是正文：

```markdown
---
title: 我的新文章
date: 2026-08-05 14:30:00
tags:
  - 标签1
  - 标签2
categories:
  - 技术
---

正文内容，支持 Markdown 语法。
```

### frontmatter 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | 是 | 文章标题 |
| `date` | 是 | 发布时间，格式 `YYYY-MM-DD HH:MM:SS` |
| `tags` | 否 | 标签列表，每行一个 `- 标签名` |
| `categories` | 否 | 分类列表，每行一个 `- 分类名` |

### Markdown 常用语法

```markdown
# 一级标题
## 二级标题
### 三级标题

**加粗**  *斜体*  `行内代码`

- 无序列表项 1
- 无序列表项 2

1. 有序列表项 1
2. 有序列表项 2

[链接文本](https://example.com)

![图片描述](图片路径)

> 引用文字

\`\`\`python
# 代码块
print("hello")
\`\`\`
```

## 七、故障排查

**问题：发布后线上没更新？**
1. 等 1-2 分钟（GitHub Pages 构建有延迟）
2. 浏览器强制刷新（Cmd+Shift+R）
3. 终端执行 `gh api --method POST 'repos/ymbkxc/ymbkxc.github.io/pages/builds'` 手动触发构建

**问题：本地预览打不开 4000？**
- 检查是否已在博客目录执行 `hexo server`
- 端口被占用就换端口：`hexo server -p 4001`

**问题：`hexo` 命令找不到？**
- 执行 `npm install -g hexo-cli` 重新安装

**问题：想改站点标题/作者？**
- 编辑 `_config.yml` 顶部 `# Site` 部分，改完 `./deploy.sh` 重新发布

## 八、后续可拓展

- 换主题（如 next、butterfly、fluid）
- 加评论系统（如 Giscus、Valine）
- 加统计（如 Google Analytics、不蒜子）
- 加图床（如 PicGo + GitHub）
- 自定义域名

需要哪个跟我说就行。

# 六时杂谈 · 写博客操作指南

> 每次写新博客时，照着这份文档一步步操作即可。
> 项目目录：`~/Documents/ymbkxc.github.io`

---

## 场景 A：写一篇新博客并发布（最常用）

### 第 1 步：打开终端，进入项目目录

```bash
cd ~/Documents/ymbkxc.github.io
```

### 第 2 步：新建文章

```bash
./new.sh 我的新文章
```

> 把"我的新文章"换成你的实际标题。
> 执行后会在 `source/_posts/我的新文章.md` 生成文件。

### 第 3 步：编辑文章

用任意编辑器（VS Code、Typora、Sublime、记事本都行）打开：

```
~/Documents/ymbkxc.github.io/source/_posts/我的新文章.md
```

文件开头长这样（已经自动填好标题和当前时间）：

```markdown
---
title: 我的新文章
date: 2026-08-05 14:30:00
tags:
categories:
---
```

**填写 tags 和 categories**，然后写正文。例如：

```markdown
---
title: 我的新文章
date: 2026-08-05 14:30:00
tags:
  - 生活
  - 随笔
categories:
  - 日常
---

今天写一篇关于……的文章。

## 第一部分

正文内容……

**重点句子加粗**。

- 要点 1
- 要点 2

## 第二部分

更多内容……
```

### 第 4 步（可选）：本地预览

发布前先看效果：

```bash
hexo server
```

浏览器打开 http://localhost:4000 查看。
预览完按 `Ctrl + C` 退出。

### 第 5 步：发布到线上

```bash
./deploy.sh
```

脚本会自动：
1. 清理旧文件
2. 生成新静态网页
3. 推送到 GitHub 的 `hexosub` 分支

等 1-2 分钟，访问 https://ymbkxc.github.io/ 即可看到新文章。

### 第 6 步：备份源码（重要，别跳过）

```bash
git add .
git commit -m "新增文章：我的新文章"
git push origin source
```

> 这一步把你的 Markdown 源码备份到 GitHub 的 `source` 分支。
> 即使本地文件丢了，也能从 GitHub 恢复。

---

## 场景 B：修改已发布的文章

### 第 1 步：找到文章

文章都在 `source/_posts/` 目录下，文件名就是文章标题。

### 第 2 步：编辑

直接修改 `.md` 文件内容。

### 第 3 步：重新发布

```bash
cd ~/Documents/ymbkxc.github.io
./deploy.sh
git add .
git commit -m "更新文章：xxx"
git push origin source
```

---

## 场景 C：删除文章

### 第 1 步：删除文件

```bash
cd ~/Documents/ymbkxc.github.io
rm source/_posts/要删除的文章.md
```

### 第 2 步：重新发布

```bash
./deploy.sh
git add .
git commit -m "删除文章：xxx"
git push origin source
```

---

## 场景 D：改站点标题、作者等信息

### 第 1 步：编辑配置

打开 `_config.yml`，修改顶部 `# Site` 部分：

```yaml
# Site
title: 六时杂谈          # ← 改这里
subtitle: ''
description: 个人博客     # ← 或改这里
keywords:
author: ymbkxc           # ← 或改这里
language: zh-CN
timezone: Asia/Shanghai
```

### 第 2 步：重新发布

```bash
./deploy.sh
git add _config.yml
git commit -m "更新站点配置"
git push origin source
```

---

## 完整示例：从零写一篇《今天的读书笔记》

```bash
# 1. 进目录
cd ~/Documents/ymbkxc.github.io

# 2. 新建
./new.sh 今天的读书笔记

# 3. 编辑文件 source/_posts/今天的读书笔记.md
#    内容如下：
```

```markdown
---
title: 今天的读书笔记
date: 2026-08-05 20:30:00
tags:
  - 读书
  - 笔记
categories:
  - 生活
---

今天读了《xxx》一书，很有启发。

## 主要内容

作者讲了三件事：

1. 第一件事
2. 第二件事
3. 第三件事

## 我的感想

**最打动我的是**：……

> "金句摘录" —— 作者

## 总结

这本书值得反复读。
```

```bash
# 4. 预览（可选）
hexo server
# 浏览器开 http://localhost:4000 看效果，Ctrl+C 退出

# 5. 发布
./deploy.sh

# 6. 备份源码
git add .
git commit -m "新增文章：今天的读书笔记"
git push origin source
```

等 1-2 分钟，https://ymbkxc.github.io/ 就能看到新文章。

---

## 常见问题

**Q：发布后线上没更新？**
A：等 1-2 分钟，然后浏览器按 `Cmd+Shift+R` 强制刷新。还不行就执行：
```bash
gh api --method POST 'repos/ymbkxc/ymbkxc.github.io/pages/builds'
```

**Q：`hexo server` 提示端口被占用？**
A：换端口：`hexo server -p 4001`

**Q：`./new.sh` 或 `./deploy.sh` 提示权限不够？**
A：赋权一次即可：
```bash
chmod +x new.sh deploy.sh
```

**Q：文章里想插入图片？**
A：把图片放到 `source/images/` 目录，然后在文章里写：
```markdown
![图片说明](/images/图片名.jpg)
```

**Q：忘了备份源码会怎样？**
A：线上博客照常显示（hexosub 分支有静态网页），但本地丢了源码就找不回 Markdown。所以**每次写完都备份**。

**Q：可以在多台电脑上写吗？**
A：可以。新电脑上：
```bash
git clone -b source https://github.com/ymbkxc/ymbkxc.github.io.git
cd ymbkxc.github.io
npm install      # 装依赖
npm install hexo-cli -g   # 装 hexo 命令
```
之后照常操作即可。

---

## 速查卡（贴在显示器边）

```bash
cd ~/Documents/ymbkxc.github.io       # 进目录
./new.sh 标题                          # 新建
# 编辑 source/_posts/标题.md
hexo server                            # 预览（可选）
./deploy.sh                           # 发布
git add . && git commit -m "新增：标题" && git push origin source  # 备份
```
